<Window x:Class="pages_and_db_v3.AddTodo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pages_and_db_v3"
        mc:Ignorable="d"
        Title="AddTodo" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Todo name" />
        <TextBox x:Name="TodoNameTextBox" Margin="10" Grid.Row="1" />

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="10">
            <Button Content="Add" Click="Add_Click"/>
            <Button Content="Cancel" Click="Cancel_Click" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
